---
import NavLink from "@components/ui/NavLink.astro";
import type { LinkData } from "@utils/types.ts";
export interface Props {
  links: LinkData[];
  intrasite?: boolean;
}

const { links, intrasite = false } = Astro.props;
---

{
  intrasite ? (
    <nav class="nav">
      {links.map((link) => (
        <NavLink {...link} />
      ))}
    </nav>
  ) : (
    <div class="nav">
      {links.map((link) => (
        <NavLink {...link} />
      ))}
    </div>
  )
}

<style>
  .nav {
    padding-bottom: var(--pad);
  }
</style>
